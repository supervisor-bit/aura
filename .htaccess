Options -Indexes
RewriteEngine On

# Statické soubory servírovat přímo
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Vše ostatní → index.php
RewriteRule ^ index.php [L]
